{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command' | grep -qE '(^|\\s|/)(npm|npx|pnpm|yarn)\\s' && echo '{\"decision\":\"block\",\"reason\":\"This project uses bun. Use bun/bunx instead of npm/npx/pnpm/yarn.\"}' || true"
          }
        ]
      }
    ]
  }
}
